BUGS
====

- core methods are generated automatically. There are methods generating
  invalid convert-syntax (e.g. resize()).

- probably some wrong or missing interface-definitions


TODO/WISHLIST
=============

- support exiftool
  * create idf input/ETinterface.txt
  * create org.im4java.core.ETOperation extends ETOps
  * create org.im4java.core.ExiftoolCmd
  * add test-case

- improve method-comments
  * add config-var "toolName" to idfs
  * use config-var "toolName" in generated files

- add documentation

- validate IM and GM interfaces

- Info
  * change to initially collect only a small number of properties
    (option of the constructor)
  * add methods returning these properties:
    int getWidth(), int getHeight(), Size getSize(), ... (TBD)
  * query the full set at first request

- Pipe
  * add method setBufferSize()
